## Global
. /etc/ksh.kshrc

## Personal

alias vi='vim'
alias la='ls -a'
alias ll='ls -lh'
alias lla='ls -lah'

# JDK 1.8
export PATH=/usr/local/jdk-1.8.0/bin:$PATH
# pip --user
export PATH=$HOME/.local/bin:$PATH
