pkg-install.txt:

  History of packages installation.

pkg-query-non-automatic.txt:

  Packages installed manually.
  Query using `pkg query -e '%a = 0' %n`, see `pkg help query`.

pkg-absent.txt:

  Packages marked expired or not available yet.
